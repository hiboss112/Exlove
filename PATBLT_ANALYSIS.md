# 🎯 PatBlt Cross-Reference 분석 결과

## 발견된 PatBlt 호출 함수들

| 함수 | PatBlt 호출 횟수 | 의심도 | 비고 |
|------|------------------|--------|------|
| **sub_41B900** | **7번** | ⭐⭐⭐⭐⭐ | **최고 의심 함수** - 여러 타일 그리기 |
| **sub_41BED0** | 2번 | ⭐⭐⭐ | 보조 렌더링 |
| **sub_41C7E0** | 2번 | ⭐⭐⭐ | 보조 렌더링 |
| sub_41B610 | 2번 | ⭐⭐ | UI 관련 추정 |
| sub_41AFB0 | 1번 | ⭐ | 단순 그리기 |
| sub_41B150 | 1번 | ⭐ | 단순 그리기 |
| sub_41B290 | 1번 | ⭐ | 단순 그리기 |
| sub_41D060 | 1번 | ⭐ | 단순 그리기 |
| sub_41D470 | 1번 | ⭐ | 단순 그리기 |

---

## 🔥 최우선 분석 대상

### sub_41B900 ← 이 함수부터 확인!

**PatBlt를 7번 호출하는 이유:**
1. 맵 타일을 여러 개 그림
2. 타일 인덱스에 따라 다른 색상
3. **타일 인덱스 -2일 때 빨간색!**

**주소:**
- +0x18B (첫 번째 호출)
- +0x1CE (두 번째 호출)
- +0x218 (세 번째 호출)
- +0x3C3 (네 번째 호출)
- +0x400 (다섯 번째 호출)
- +0x444 (여섯 번째 호출)
- +0x482 (일곱 번째 호출)

---

## 📋 IDA Pro에서 지금 실행할 명령

### Step 1: sub_41B900 디컴파일
```
1. Ctrl + G → "41B900" 입력
2. F5 (디컴파일)
3. 전체 코드 확인
```

### Step 2: 빨간색 상수 찾기
디컴파일된 코드에서 검색:
- `0xFF` 또는 `255` (빨간색 R값)
- `0x000000FF` (RGB 빨간색, Little Endian)
- `RGB(255, 0, 0)`
- `-2` 또는 `0xFFFFFFFE` (타일 인덱스)

### Step 3: 주변 코드 패턴 확인
```c
// 예상 패턴
if (tile_index == -2) {
    // 이 근처에 PatBlt 호출
    PatBlt(hdc, x, y, 32, 32, PATCOPY);
}
```

---

## 🎯 다음 액션

**지금 IDA Pro에서:**

```
1. 주소 41B900로 이동
2. F5 키 (디컴파일)
3. 전체 함수 코드 복사
4. 특히 PatBlt 호출 전후 코드 주목!
```

**이 함수가 90% 확률로 빨간색 타일을 그리는 코드입니다!** 🎯

---

## 💡 참고: 다른 함수들도 확인할 것

만약 sub_41B900에서 못 찾으면:
- **sub_41BED0** (2번 PatBlt)
- **sub_41C7E0** (2번 PatBlt)

순서대로 디컴파일하세요.

---

## 🔍 예상되는 코드 구조

### 시나리오 A: 타일 타입별 색상
```c
void sub_41B900(...) {
    // 7번의 PatBlt = 7가지 타일 타입?
    switch (tile_type) {
        case -2:  // 유효하지 않은 타일
            SelectObject(hdc, red_brush);
            PatBlt(hdc, x, y, 32, 32, PATCOPY);  // 빨간색!
            break;
        case 0:   // 일반 타일
            PatBlt(...);
            break;
        // ... 다른 타입들
    }
}
```

### 시나리오 B: 레이어별 렌더링
```c
void sub_41B900(...) {
    // 레이어 1: 배경 (빨간색으로 초기화)
    PatBlt(hdc, 0, 0, 800, 600, PATCOPY);  // 빨간색 전체
    
    // 레이어 2-7: 각종 타일들
    PatBlt(...);
    PatBlt(...);
    // ...
}
```

---

**sub_41B900 함수의 디컴파일 결과를 보여주세요!**