# IDA Pro 맵 붉게 표시 문제 분석 체크리스트

## 1단계: 렌더링 함수 역공학

### sub_401B60 분석
- [ ] F5로 디컴파일하여 렌더링 로직 확인
- [ ] DirectX/OpenGL 함수 호출 확인 (예: DrawPrimitive, SetTexture)
- [ ] 타일 텍스처 바인딩 부분 찾기
- [ ] 에러 처리 시 기본 색상 확인 (0xFF0000 검색)

### sub_401BF0 분석
- [ ] 맵 타일 그리기 루프 확인
- [ ] 텍스처 인덱스 범위 체크 확인
- [ ] 잘못된 타일 ID에 대한 폴백 처리 확인

### sub_40F780 분석 (타일셋 로딩)
- [ ] 파일 경로 처리 로직
- [ ] 이미지 로딩 실패 시 처리 (NULL 체크)
- [ ] 기본 텍스처 생성 부분 (빨간색으로 채우는지 확인)

## 2단계: 데이터 구조체 리버싱

### 맵 객체 구조체 (this+10 포인터)
```
offset +104: 맵 ID
offset +108: X 좌표 (% 800)
offset +112: Y 좌표 (% 600)
offset +124: 타일 X 인덱스
offset +128: 타일 Y 인덱스
offset +3129: 텍스처 경로 1 [65 bytes]
offset +3194: 텍스처 경로 2 [65 bytes]
offset +3260: 맵 플래그
```

- [ ] IDA에서 구조체 정의 생성 (Structures 창)
- [ ] 각 오프셋의 타입 확인 및 변수명 지정
- [ ] Cross-reference로 다른 사용 위치 추적

## 3단계: 문자열 및 상수 검색

### IDA Strings 윈도우 (Shift+F12)
- [ ] "texture", "tile", "map" 검색
- [ ] ".bmp", ".tga", ".dds" 등 이미지 확장자 검색
- [ ] 에러 메시지 확인

### 16진수 값 검색 (Alt+I)
- [ ] `0x00FF0000` (빨간색 ARGB)
- [ ] `0xFF0000` (빨간색 RGB)
- [ ] `800`, `600` (타일 크기 상수)

## 4단계: 호출 그래프 분석

### sub_40F780 역추적
- [ ] Ctrl+X로 호출하는 함수 목록 확인
- [ ] 파일 로딩 함수까지 역추적
- [ ] 실패 시 반환값 확인 (0, -1, NULL 등)

### 렌더링 파이프라인 추적
```
sub_459790 (패킷 핸들러)
  └─> sub_401B60 (맵 렌더링)
      └─> sub_XXXXXX (타일 그리기)
          └─> DirectX/OpenGL 함수
```

## 5단계: 디버깅 포인트 설정

### 브레이크포인트 후보
- [ ] `sub_401B60` 진입점
- [ ] `sub_40F780` 반환 직전 (성공/실패 확인)
- [ ] case 0x86의 좌표 계산 부분
- [ ] 타일 인덱스가 범위를 벗어나는지 체크

## 6단계: 가능한 원인 및 해결 방법

### 원인 1: 텍스처 파일 누락
- **증상**: 타일셋 이미지 로딩 실패
- **확인**: sub_40F780에서 파일 경로 및 로딩 결과 확인
- **패치**: 기본 텍스처를 흰색/회색으로 변경

### 원인 2: 타일 인덱스 범위 초과
- **증상**: 잘못된 타일 ID로 렌더링 시도
- **확인**: v108, v109, v110 값이 유효 범위 내인지
- **패치**: 범위 체크 로직 강화

### 원인 3: 렌더링 상태 오류
- **증상**: DirectX/OpenGL 상태 머신 문제
- **확인**: SetRenderState, SetTexture 호출 순서
- **패치**: 상태 초기화 코드 추가

### 원인 4: 서버 데이터 오류
- **증상**: 패킷에서 잘못된 맵 데이터 수신
- **확인**: 패킷 덤프로 opcode 0x86 데이터 확인
- **패치**: 클라이언트 측 검증 로직 추가

## 7단계: 패치 작성

### 예시: 기본 색상 변경
```asm
; 원본 (빨간색)
mov eax, 0FF0000h

; 패치 (회색)
mov eax, 808080h
```

### 예시: 텍스처 로딩 실패 시 처리
```c
// IDA 의사코드에서 다음과 같은 패턴 찾기:
if ( !texture_loaded )
  color = 0xFF0000;  // <- 이 부분을 0xFFFFFF로 변경
```

---

## 빠른 시작 가이드

1. **sub_401B60** 더블클릭 → F5 디컴파일
2. **0xFF0000** 검색 (Alt+T → Immediate value)
3. Cross-reference 확인 (Ctrl+X)
4. 해당 위치를 NOP 또는 다른 색상으로 패치
5. Edit → Patch Program → Apply patches to input file

---

## ⚠️ 발견된 문제점

### 🔴 CRITICAL: sub_401B60에서 타일 초기화 문제 발견!

```c
memset32(this + 18, -2, 0x1Eu);
// this+18 위치에 0xFFFFFFFE (-2)를 30개(0x1E) 채움
```

**분석**:
- `this + 18` = 오프셋 72부터 시작하는 배열
- `-2` (0xFFFFFFFE) = 잘못된 타일 인덱스
- `0x1E` (30개) = 타일 배열 크기

**가설**: 
타일 인덱스가 -2일 때, 렌더링 엔진이 "유효하지 않은 타일"로 인식하고 
**빨간색으로 표시**하는 것으로 추정됩니다!

### 🔍 추가 분석 필요 함수

#### sub_40F810 (타일 플래그 변환)
```c
v4 = *(_BYTE *)(v3 + a2);  // 타일셋 데이터에서 1바이트 읽기
this[781] |= sub_40F810(v4);  // 플래그 변환 및 OR 연산
```
→ 각 타일의 속성을 비트마스크로 변환하는 함수

#### sub_432A10 (타일 로딩 실행)
```c
return sub_432A10(this[781]);  // 최종 플래그로 타일 로드
```
→ 실제 타일셋 이미지를 로딩하는 함수 (여기서 실패 가능성!)

#### sub_4013D0, sub_401EE0 (렌더링 초기화)
```c
sub_4013D0(this);  // 렌더링 준비
return sub_401EE0(this);  // 실제 렌더링
```
→ 타일 인덱스 -2를 어떻게 처리하는지 확인 필요

---

## 🔬 추가 분석 결과

### sub_401EE0 분석 완료
```c
// 타일 좌표 계산
word_48B610 = dword_48B5E0 >> 5;  // X 타일 인덱스 (÷32)
word_48B612 = dword_48B5E4 >> 5;  // Y 타일 인덱스 (÷32)

// 오브젝트/NPC 렌더링
if ( PtInRect(&rc, point) )
    sub_401970(v3);  // ← 오브젝트 그리기
```
**결론**: 이 함수는 오브젝트만 처리하고, **타일 자체는 그리지 않음**

### sub_40F810 분석 완료 (타일 속성 매핑)
```
'S' → 0x1      (일반 타일)
'K' → 0x2      (벽/충돌)
'd' → 0x4      (문)
'C' → 0x8      (성)
'F' → 0x20     (숲)
'O' → 0x200    (오브젝트)
'B' → 0x100    (다리)
'T' → 0x800    (덫)
'A' → 0x8000   (애니메이션)
...
```
**결론**: 타일 속성만 변환, 색상과 무관

### ⚠️ 아직 찾지 못한 핵심 함수!

**실제 타일을 화면에 그리는 메인 루프**를 찾아야 합니다!

---

## 🎯 다음으로 분석할 함수 (우선순위)

### 1️⃣ sub_401970 (오브젝트 렌더링)
```c
// sub_401EE0에서 호출됨
sub_401970(v3);  // v3 = 오브젝트 인덱스
```
**확인사항**: 이 함수가 타일도 그리는지, 아니면 오브젝트만 그리는지

### 2️⃣ sub_432A50 (타일셋 이미지 로딩) ✅ 분석 완료
```c
// 3x9 = 27개의 타일셋 처리
for (v6 = 3; v6 > 0; v6--) {
    for (v3 = 9; v3 > 0; v3--) {
        if (*(_DWORD *)v4) {  // 타일셋이 있으면
            if (sub_432AB0(*(_DWORD *)v4)) {  // 로딩 성공
                *((_DWORD *)v4 + 8) = *((_DWORD *)v4 + 7) + 2;
            } else {  // 로딩 실패!
                sub_432AD0(v2);  // ← 빨간색 텍스처 생성 가능성!
            }
        }
    }
}
```

**🔴 핵심 발견!**
- `sub_432AB0`: 비트마스크 검증 함수 (`this[411] & a2`)
- `sub_432AD0`: 타일셋 유효성 검증 (복잡한 조건 체크)

**⚠️ 중요**: 이 함수들은 빨간색을 직접 그리지 않음! 
실제 렌더링 함수를 찾아야 함!

### 3️⃣ 타일 메인 렌더링 루프 찾기

**예상 패턴**:
```c
for (y = 0; y < map_height; y++) {
    for (x = 0; x < map_width; x++) {
        tile_id = map_data[y][x];
        if (tile_id == -2) {
            // ← 여기서 빨간색 설정!
            DrawRedTile(x, y);
        } else {
            DrawTile(x, y, tile_id);
        }
    }
}
```

**IDA에서 찾는 방법**:
1. **중첩 루프 패턴 검색**: 25x19 (800÷32 × 600÷32) 크기의 루프
2. **>> 5** (÷32) 연산 검색: 타일 좌표 계산
3. **DirectX/GDI 함수 호출 검색**:
   - `IDirect3DDevice::DrawPrimitive`
   - `StretchBlt`, `BitBlt` (GDI)
   - `SetPixel`, `FillRect`

---

## 🔍 IDA Pro에서 지금 실행할 명령

### ⚠️ 중요: Import 테이블 분석 완료!
**이 게임은 DirectDraw + GDI를 사용합니다.**
- ✅ PatBlt 있음 (단색 채우기)
- ❌ BitBlt 없음
- ❌ StretchBlt 없음
- ❌ FillRect 없음

### 방법 1: PatBlt 검색 (최우선!) ⭐⭐⭐⭐⭐
```
Ctrl + F → "PatBlt"
→ Import 주소: 0048105C
→ Ctrl + X (호출하는 함수 확인)
→ F5 (디컴파일)
```

### 방법 2: SelectObject + CreateSolidBrush 패턴 검색
```
Ctrl + F → "SelectObject"
Ctrl + F → "CreateSolidBrush" (없으면 상수 검색)
Alt + I → 255 (RGB 빨간색 R값)
```

### 방법 3: 상수 검색으로 타일 크기 찾기
```
Alt + I (Immediate value search)
검색: 32 (0x20) - 타일 크기
또는: 25 (0x19) - 가로 타일 개수
또는: 19 (0x13) - 세로 타일 개수
```

### 방법 4: Cross-reference로 추적
```
1. memset32 함수로 이동 (Ctrl + F)
2. this+18 배열을 읽는 코드 찾기 (Ctrl + X)
3. 해당 배열을 사용하는 렌더링 함수 확인
```

---

**생성일**: 2025-10-07  
**대상 게임**: ExLove  
**문제**: 맵이 붉게 표시됨  
**분석 파일**: sub_459790 (네트워크 패킷 핸들러)